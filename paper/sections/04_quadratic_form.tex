\section{Quadratic Form}

This section will express cumulative P\&L as
\[
P_{t,t_0} = \frac{1}{2} r^\top M_q^{(t,t_0)} r
\]
for a Gaussian return vector \(r\). It will define the EWM memory matrix, the trading-window selector, the symmetric P\&L matrix, and the OU return covariance matrix.
